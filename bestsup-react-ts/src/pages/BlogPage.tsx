import * as React from 'react';

import BlogContainer from 'containers/BlogContainer';

const BlogPage: React.StatelessComponent<{}> = () => {
  return (
    <main>
      <BlogContainer />
    </main>
  );
}

export default BlogPage;
